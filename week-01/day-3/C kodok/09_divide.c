#include <stdio.h>

int main() {
	int a = 53625;
	int b = 11;

if (a % b != 0)
   printf("The remainder is: %i", a%b);
else printf("a can b divided without reminder");
	// tell if it has 11 as a divisor
	return 0;
}
